import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
    InputText: {
        borderColor: "#fff",
        borderWidth: 1,
        padding: 10,
        marginBottom: 10,
        textAlign: "center"
    }
})

export {
    styles
}
