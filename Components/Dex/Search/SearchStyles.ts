import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
    InputText: {
        backgroundColor: "",
        borderColor: "grey",
        borderWidth: 1,
        padding: "10px",
        marginBottom: "10px",
        textAlign: "center"
    }
})

export {
    styles
}